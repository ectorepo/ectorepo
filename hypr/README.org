#+title:     Ectorepo: Hypr
#+author:    David Conner
#+email:     noreply@te.xel.io
#+PROPERTY: header-args+ :comments none

When using =repo init -u $url -m $manifest= instead of the git submodule to
checkout, the filepaths for =<include name=mnfst.xml/>= are relative to the root
repository.

#+begin_src xml :tangle default.xml
<manifest>
  <include name="_remotes.xml"/>
  <default remote="github" sync-j="8" revision="refs/heads/master"/>
  <include name="hypr/hyprwm.xml"/>
  <include name="hypr/hyprcommunity.xml"/>
</manifest>
#+end_src

** Babel Setup

To avoid confirmations

#+begin_src emacs-lisp
(setq-local org-confirm-babel-evaluate nil)
#+end_src

Load blocks

#+begin_src emacs-lisp
(org-babel-lob-ingest "../babel.org")
#+end_src

#+RESULTS:
: 1

Check rates with =ghubCheckRates()=

#+call: ghubCheckRates()

#+RESULTS:
#+begin_src emacs-lisp
5000
#+end_src

* Hyprwm

#+name: hyprwmLogin
#+begin_src emacs-lisp
"hyprwm"
#+end_src

Excluded Repositories

#+NAME: hyprwmReposExclude
| .github          |
| standards        |
| hyprland-website |

#+name: hyprwmReposFetch
#+call: ghubReposFetch(login=hyprwmLogin)

Clone bundle sizes

#+name: hyprwmReposBundles
#+call: ghubReposBundles(repos=hyprwmReposFetch)

Clone bundles total

#+name: hyprwmReposBundlesTotal
#+call: ghubReposBundlesTotal(tbl=hyprwmReposBundles[,0])

#+RESULTS: hyprwmReposBundlesTotal
: 315221

** Hyprwm Repos

Filter the results, generate XML

#+name: hyprwmReposXML
#+call: ghubReposXML(repos=hyprwmReposFetch, reposExclude=hyprwmReposExclude, login=hyprwmLogin)

** Generate XML

Generate =hyprwm.xml=

#+begin_src xml :tangle hyprwm.xml :noweb yes
<manifest>
  <<hyprwmReposXML()>>
</manifest>
#+end_src

* Hyprcommunity

#+name: hyprcommunityLogin
#+begin_src emacs-lisp
"hyprland-community"
#+end_src

Excluded Repositories

#+NAME: hyprcommunityReposExclude
| .github          |
| standards        |
| hyprland-website |

#+name: hyprcommunityReposFetch
#+call: ghubReposFetch(login=hyprcommunityLogin)

Clone bundle sizes

#+name: hyprcommunityReposBundles
#+call: ghubReposBundles(repos=hyprcommunityReposFetch)

Clone bundles total

#+name: hyprcommunityReposBundlesTotal
#+call: ghubReposBundlesTotal(tbl=hyprcommunityReposBundles[,0])

#+RESULTS: hyprcommunityReposBundlesTotal
: 315221

** Hyprcommunity Repos

Filter the results, generate XML

#+name: hyprcommunityReposXML
#+call: ghubReposXML(repos=hyprcommunityReposFetch, reposExclude=hyprcommunityReposExclude, login=hyprcommunityLogin)

** Generate XML

Generate =hyprcommunity.xml=

#+begin_src xml :tangle hyprcommunity.xml :noweb yes
<manifest>
  <<hyprcommunityReposXML()>>
</manifest>
#+end_src
